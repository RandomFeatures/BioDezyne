Public Class frmPrintPreview

    Private Sub frmPrintPreview_Load(ByVal sender As Object, ByVal e As System.EventArgs) Handles Me.Load
        'Dim rpt As New rptpatientmain
        'rpt.PageSettings.Margins.Left = 0.5
        'rpt.PageSettings.Margins.Right = 0.5
        'rpt.PageSettings.Margins.Top = 0.5
        'rpt.PageSettings.Margins.Bottom = 0.5


        'rpt.Run(False)
        'rptViewer.Document = rpt.Document

        ''        rpt.Document.Print(False, False, False)
        ''If objSaveFile.ShowDialog() = DialogResult.OK Then
        ''    PdfExport.Export(rpt.Document, objSaveFile.FileName)
        ''End If
        ''rpt.Document.Dispose()
        ''rpt.Dispose()
    End Sub
End Class
